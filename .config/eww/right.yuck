(include "widgets/timer.yuck")
(include "widgets/sound.yuck")
(include "widgets/network.yuck")
(include "widgets/bluetooth.yuck")
(include "widgets/battery.yuck")
(include "widgets/kb_layout.yuck")

;; Main Widget
(
  defwidget right [] (
    box
    :halign "end"
    :spacing 10
    :space-evenly false
    (timer)
    (sound)
    (network)
    (bluetooth)
    (battery)
    (kbLayout)
  )
)